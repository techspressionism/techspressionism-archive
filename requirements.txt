# Stage 3 (Whisper) and the yt-dlp fetches need these; Stages 2, 4, 5 are standard-library only.
# Python 3.10+ required (mlx, yt-dlp). Recreate with:
#   uv venv --python 3.12 .venv && uv pip install --python .venv/bin/python -r requirements.txt
#   ln -sf "$(.venv/bin/python -c 'import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())')" .venv/bin/ffmpeg
yt-dlp
mlx-whisper
imageio-ffmpeg   # bundles the ffmpeg binary (no Homebrew needed)

# Stage 6 (site build) needs this one on its own -- PDF transcript downloads (2026-09-22). Deliberately NOT
# installed via this whole file in CI (.github/workflows/deploy.yml installs it directly): mlx-whisper above is
# Apple Silicon-only and would fail to install on the Linux runner, which never needs it anyway (Stage 6 doesn't
# touch Whisper/yt-dlp at all).
fpdf2
