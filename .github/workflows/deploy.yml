name: Deploy search site to GitHub Pages

on:
  push:
    branches: [main]
    # a push that only changes the manual or README does not change the site, so it need not redeploy it
    paths-ignore:
      - "docs/**"
      - "README.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Refresh the techspressionism.com menu (the strip at the top of every page)
        # reads the public home page once; if that fails the copy already in the repository (data/wp-menu.json) is used
        run: python3 scripts/refresh-wp-menu.py || echo "menu not refreshed; using the committed copy"
      - name: Build site + Pagefind index
        # this GitHub copy is the test site: it always asks search engines not to list it, even after techspressionism.com/archive/ is opened to them
        env:
          TVA_NOINDEX: "1"
        run: python3 scripts/06-build-site.py
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
